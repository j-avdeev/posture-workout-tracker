/**
 * Optional Supabase configuration.
 * These browser-facing values are safe to publish only when Row Level Security
 * is enabled with the policies from supabase.sql.
 */
window.APP_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_PUBLISHABLE_KEY: ''
};
